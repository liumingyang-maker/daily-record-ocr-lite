# v1.0.1 Qwen3.7 Plus P0 热修复审计报告

## 一键复制给 GPT 审查

```text
请作为独立软件发布审计员，审查 daily-record-ocr-lite v1.0.1 Qwen3.7 Plus P0 热修复。

仓库：
https://github.com/liumingyang-maker/daily-record-ocr-lite

比较范围：
v1.0.0...v1.0.1

PR：
https://github.com/liumingyang-maker/daily-record-ocr-lite/pull/2

Release：
https://github.com/liumingyang-maker/daily-record-ocr-lite/releases/tag/v1.0.1

完整审计报告：
https://github.com/liumingyang-maker/daily-record-ocr-lite/blob/master/docs/audits/V1.0.1_QWEN37_P0_AUDIT.md

请分别按以下两条轴审查：

1. 项目规范
- 阅读 AGENTS.md、docs/RELEASE_CHECKLIST.md、.gitignore、pyproject.toml。
- 检查 Secret、本机配置、真实测试、CI、Release Gate 和资产是否符合规范。

2. 任务书符合性
- 核验 json_object、enable_thinking=false。
- 核验 strict JSON、Markdown fenced JSON、embedded JSON 的解析顺序。
- 核验 vision_capability、json_response_capability、strict_json_capability 独立。
- 核验 marker 未被修补，response_preview 不超过 500 字符且已脱敏。
- 核验真实 qwen3.7-plus 结果、Secret 零提交、merge commit 和四项 Release 资产。
- 检查是否混入 OCR 预热、多核、Mac 适配或 Pipeline 重构。

已知记录：
- 真实结果：HTTP 200，3532ms，status OK，三项能力均为 true，marker VISION-7319。
- P2 已知限制：失败路径尚未透传真实上游 http_status。
- 本次审计是发布后追溯审计；后续必须在合并和发布前审计。
- v1.0.1 Tag 内 Checklist 保留发布时的未勾选状态和 PR #1 引用；GitHub Release 已增加勘误。
- SHA256SUMS.txt 只正式覆盖源码 ZIP；允许的非敏感 golden_cases.yaml 位于 private 路径。

输出格式：
- 阻塞/严重问题
- 重要问题
- P2/轻微问题
- 规范审查结论
- 任务书审查结论
- 是否建议维持 v1.0.1 发布结论

每项发现必须给出文件、行号或可核验证据。不要输出或索取任何 API Key。
```

## 审计元数据

- 审计范围：`v1.0.0...v1.0.1`
- 基线 Tag：`v1.0.0`（`3669982f55cd26dd963553e75a78b620d4f3b2bd`）
- 热修复分支 SHA：`9581f4b52a73b092df1e8f59f3d2f2c09b2381ca`
- PR：[PR #2](https://github.com/liumingyang-maker/daily-record-ocr-lite/pull/2)
- Merge SHA：`081ed49b1b32263efba2368771c9f2d689cee427`
- Release commit / Tag target：`7023ea6ffabb1b099f04a52387e3cce1cba551c5`
- Release：[daily-record-ocr-lite v1.0.1](https://github.com/liumingyang-maker/daily-record-ocr-lite/releases/tag/v1.0.1)
- 审计日期：2026-07-24

## 任务范围与禁止项

本次仅发布 Qwen3.7 Plus P0 热修复：

- 修复 HTTP 200 被误判为 `FAILED_JSON_CAPABILITY`。
- 为阿里云兼容端点的 `qwen3.7-plus` 发送 `json_object` 和 `enable_thinking=false`。
- 支持严格 JSON、Markdown 围栏 JSON 和说明文字内嵌 JSON。
- 独立判断视觉、JSON 响应和严格 JSON 三项能力。
- 返回最多 500 字符的脱敏响应预览。

明确未包含 OCR 预热、多核、Mac 适配或 Pipeline 重构。

## 实现审计

- `lite_app/vision/openai_compatible.py`
  - 仅对 `*.aliyuncs.com` 且模型为 `qwen3.7-plus` 的请求强制使用 `json_object`。
  - 默认发送 `enable_thinking=false`；真实接口已在关闭思考时成功，因此未增加最低思考回退。
- `lite_app/vision/probe.py`
  - 解析顺序为原始 `json.loads`、移除 JSON 围栏、提取首个完整 JSON 对象。
  - 不使用正则修补字段，不修改 marker。
  - 响应预览过滤已知 Secret 和 Authorization 行。
- `lite_app/main.py`
  - 三项能力不再共用布尔值。
  - marker 正确即返回 `status=OK` 和 `vision_capability=true`。
  - JSON 正确但 marker 错误时单独返回视觉能力失败。

## 验证证据

- 本地：`ruff check .` 通过。
- 本地：`git diff --check` 通过。
- 本地：`python -m pytest -m "not real_ocr" -q` 为 `247 passed, 5 deselected`。
- PR #2：Python 3.11、Python 3.12、Paddle inference、Windows install 全部成功。
- master：CI、Real OCR、Windows install smoke 全部成功。
- Tag Release：core、real-ocr、windows-install、publish 全部成功。
- 本地 Release source Gate：本机配置存在，`doctor_state=READY`、`ready=true`。
- GitHub Release source Gate：无用户凭据环境，`doctor_state=SETUP_REQUIRED`、`ready=true`，符合预期。

## 真实 Qwen3.7 Plus 验收

- Model：`qwen3.7-plus`
- HTTP：`200`
- Latency：`3532ms`
- Status：`OK`
- Vision capability：`true`
- JSON response capability：`true`
- Strict JSON capability：`true`
- Marker：`VISION-7319`

首次真实调用出现一次未复现的 Provider/Network 错误；随后直接调用与页面接口均成功。该瞬时错误未被伪装为 JSON 能力成功。

## 安全审计

- `git grep -n -E 'sk-(ws|sp)-[A-Za-z0-9._-]+'`：无匹配。
- `git diff | grep -E 'sk-(ws|sp)-[A-Za-z0-9._-]+'`：无匹配。
- `data/settings.json`、`data/secrets.env`、`data/setup_health.json`、`data/connection-test.png` 和 `data/jobs/**` 均由 `.gitignore` 排除。
- 本机配置文件未删除。
- 公共设置 API 无法回读 Key。
- 从 GitHub 下载的源码 ZIP 与 `SHA256SUMS.txt` 一致；GitHub 资产元数据为四项资产公开提供 digest，但发布的校验清单只覆盖源码 ZIP。
- Release ZIP 共 139 个条目；敏感条目检查结果为 0。
- ZIP 包含允许的非敏感文件 `tests/fixtures/private/golden_cases.yaml` 和 `.gitkeep`，不包含真实私密图片、API Key、用户配置或原始 API 响应。

## Release 资产

- `daily-record-ocr-lite-v1.0.1-source.zip`
- `SHA256SUMS.txt`
- `INSTALL_WITH_AI.txt`
- `RELEASE_NOTES.md`

## 独立 GPT 审查

### 项目规范审查

发现 2 项重要问题和 1 项判断项：

1. **重要，已勘误**：不可变的 v1.0.1 Tag 内 `docs/RELEASE_CHECKLIST.md` 未勾选并残留 PR #1。发布后的 master 已依据实际证据更正为 PR #2；GitHub Release 正文和当前 Release Notes 已明确说明 Tag 内历史错误。未移动或重打 Tag。
2. **重要，已修复**：初稿将本地 `doctor_state=READY` 写成 Release CI 结果。已根据 GitHub 原始日志更正为：本地为 READY，GitHub 无凭据环境为 `SETUP_REQUIRED` 且 `ready=true`。
3. **判断项，接受本次例外**：本报告与审计规则是在 v1.0.1 发布完成后补建，属于追溯审计，无法满足新规则的“完成前审计”。该例外仅适用于本次；后续任务必须在合并和发布前完成报告与独立 GPT 复核。

其余无规范发现。独立 GPT 已核验提交范围、Secret 扫描、私密文件排除、PR/master/Tag CI、真实 OCR、Release Gate、四项资产和禁止范围。

### 任务书符合性审查

发现 1 项 P2：

1. **P2，接受为 v1.0.1 已知限制**：`lite_app/main.py` 成功路径的 `http_status` 固定为 200，异常路径固定为 null；Provider 获得的真实失败状态没有透传。该问题不影响本次“上游 HTTP 200 被误判为 JSON 失败”的 P0 修复，且真实验收的 200 值正确。为避免已发布后扩大 Provider 接口，本次不追加代码变更；后续诊断改进应透传真实上游状态。

其余无任务书发现。独立 GPT 已核验 `json_object`、关闭思考、三阶解析、三项能力独立、marker 不修补、500 字符脱敏、真实结果、Secret 零命中、merge commit、四项 Release 资产以及无禁止范围漂移。

### 外部 GPT 发布复审

外部 GPT 建议维持 v1.0.1，不撤销 Release、不删除或移动 Tag，并提出以下证据修正：

1. **重要，已勘误**：Tag 内 Checklist 未勾选且误写 PR #1。当前 master、Release Notes 和 GitHub Release 正文明确区分不可变 Tag 与追溯清单。
2. **重要，已修复**：原报告将四项资产统称为 SHA-256 校验通过。现已更正为：`SHA256SUMS.txt` 正式覆盖源码 ZIP，GitHub 资产元数据另行提供四项 digest。
3. **重要，已修复**：原报告“私密条目为 0”含义不清。现已说明 ZIP 允许包含非敏感的 `golden_cases.yaml` 和 `.gitkeep`，敏感条目为 0。
4. **P2，已记录**：上游失败状态未透传；阿里云 Provider 条件略宽且缺少负向测试；Authorization 脱敏形式可加强；Python 元数据未限制 `<3.13`；真实 Qwen 尚无公开可重复 Gate。

后续版本应补充负向 Provider 测试、统一脱敏器、真实 Qwen Secret Workflow、更完整的 Release 校验清单，并明确 Python 支持上限。这些增强不改变 v1.0.1 P0 结论。

## 当前结论

独立 GPT 双轴审查与外部 GPT 发布复审均已完成：无阻塞或严重问题；Tag Checklist 历史错误已通过 Release 勘误闭环，资产校验和敏感条目表述已修正，后续 P2 已记录。v1.0.1 的 P0 发布结论保持通过。
