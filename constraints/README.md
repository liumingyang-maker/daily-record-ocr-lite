# Constraints

Core and OCR ranges are in `requirements*.txt`. Platform-specific Paddle GPU commands must
follow the current official compatibility matrix and are intentionally not pinned to a stale
CUDA wheel URL here.
